<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/bgbildauth"
    tools:context=".ui.RecipeListFragment">

    <EditText
        android:id="@+id/recipeListEditTextSearch"
        android:layout_width="280dp"
        android:layout_height="46dp"
        android:layout_gravity="center"
        android:layout_marginStart="16dp"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="13dp"
        android:autofillHints="One Piece"
        android:background="@drawable/search_edit_text_background"
        android:hint="@string/suchen"
        android:textColorHint="@color/white"
        android:imeOptions="actionSearch"
        android:inputType="textCapWords"
        android:paddingStart="20dp"
        android:paddingTop="8dp"
        android:paddingEnd="20dp"
        android:paddingBottom="8dp"
        android:textColor="#FFFFFF"
        android:textSize="22sp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/recipeListSearchButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="Suchen..."
        android:backgroundTint="#8DC84E"
        android:textColor="@color/white"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintLeft_toRightOf="@+id/recipeListEditTextSearch"
        app:layout_constraintTop_toTopOf="parent" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/reciperListRV"
        android:layout_width="409dp"
        android:layout_height="657dp"
        android:layout_marginTop="16dp"
        android:padding="19dp"
        app:spanCount="2"
        tools:listitem="@layout/item_recipe"
        app:layoutManager="androidx.recyclerview.widget.GridLayoutManager"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/recipeListSearchButton" />


</androidx.constraintlayout.widget.ConstraintLayout>